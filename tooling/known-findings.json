{
  "_comment": "QA findings that are analysed and awaiting a reviewed fix. Listing one downgrades it ERROR->WARN and annotates it; a finding NOT listed here fails the gate. This file is empty by design: every finding recorded against the imported baseline has been fixed, so nothing is currently tolerated."
}
